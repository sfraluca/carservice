<?php

namespace App\Entities;

use App\CarProduct;

class RegisterProduct
{

    public function registerProduct($params)
    {
        // $carservice = CarService::create([
        //     'description' => $params['description'],
        //     'price' => $params['price'],
        // ]);

      //  $carservice->category()->attach($params['category_id']);
        
        return $carservice;
    }
}